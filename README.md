# Uni Connect (Student Chat)

No database. Profile is stored client-side (localStorage). Real-time chat uses WebRTC DataChannel with in-memory signaling on the server.

## Run

1. Install deps: `pnpm i` (or `npm i`)
2. Dev: `pnpm dev`

